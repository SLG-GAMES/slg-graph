{
  "name": "assets_test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ assets_test",
    "create-local": "graph create --node http://127.0.0.1:8020/ assets_test",
    "remove-local": "graph remove --node http://127.0.0.1:8020/ assets_test",
    "deploy-local": "graph deploy --node http://127.0.0.1:8020/ --ipfs http://127.0.0.1:5001 assets_test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}
